# 検証記録 — 1.0.1 / code 2

検証日：2026-09-08。前版の記録は [1.0.0](validation-1.0.0.md)。

## ビルドとデータ

- `assembleDebug`、`assembleDebugAndroidTest`、`bundleRelease`：成功。
- 単体テスト：7件成功。座標変換、破損形態の拒否、接続データの解釈。
- `lintRelease`：エラー0、警告17件。新しい依存バージョン、SDK、スタイル等の推奨。
- Pythonデータ検証：8件成功。
- 全90領域の全7,712,880三角形と座標が、公式ソースと順序を含めて一致。
- 操作用の各領域は最大20,000三角形。原領域からの境界のずれを検査。
- 概観の枝2,237,417本が、同梱523細胞の元ファイルの全枝数と一致。
- 165,122細胞、25,563,197接続、124,025,046シナプス、双方向索引の検証も継続。
- AABと全データ入りAPKの全711アトラスファイルをマニフェストのSHA-256と照合。

## エミュレーター

API 35 / x86_64 / SwiftShader。ジェスチャー操作はエミュレーターのみ。
計装テスト：**6件成功**（最終版65.945秒）。

1. 全領域の元の形状、概観の全枝、GPU容量上限、メモリ・描画時間。
2. 縦画面の境界ドラッグ、3D最大化と復帰、横画面の境界ドラッグ、
   縦横それぞれの割合保存、回転後の視点維持、Activity再生成後の保存。
3. 部位検索・解説・保存、Activity再生成後の保存状態。
4. 1本指回転、2本指ピンチ、視点リセット。
5. 神経11503の公式配布元からの取得とキャッシュの一致。
6. 神経10001の検索・元形態、入力・出力、接続相手側の同じ辺の照合。

実際の署名付きAABをbundletoolで分割インストールし、`split_atlas_models.apk` と
AssetManagerによる高精細データの読み込みを確認。
配布用universal APKについても、同じエミュレーターにインストールして表示・境界操作を確認。
画面は `docs/screenshots/1.0.1/` に保存。

## 配布物と実機

- 同じAABから生成した全データ入りAPKと、Play用AABを用意。
- `bundletool validate`、APK署名、16KB ZIPアラインメントを検証。
- 署名鍵は前版と同じ専用鍵。秘密情報はGit・配布物に含めない。
- 正確なサイズ・SHA-256：[release-1.0.1.json](play/release-1.0.1.json)。
- Pixel 10a（8GB、Android 17）には検証済みAPKを `install -r` で更新。
  アプリデータ消去・アンインストール・タッチ入力注入は行っていない。
  インストール成功、version 1.0.1/code 2、APKハッシュ一致、前面での起動、
  全7,712,880三角形の描画を確認。静止状態のPSSは492.2MiB。
- [メモリと精細度の記録](PERFORMANCE.md)。

## 検証の限界

全8GB端末・全GPU・全Androidバージョンでの互換性を保証する測定ではない。
実機でのジェスチャー中のFPS、長時間・低メモリ状態での使用、全神経のオンライン形態の
可用性、専門家による解説の査読は未確認。
Google Playへのアップロード・審査申請・製品版公開は未実施。
