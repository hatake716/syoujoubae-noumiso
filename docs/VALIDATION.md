# 検証記録 — 1.0.2 / code 3

検証日：2026-09-08。前版の詳細な精細度・操作・データ検証は [1.0.1](validation-1.0.1.md)。

## 今回の変更

「神経の概観」を起動時にオンにした。表示設定でオフにできる。
セッション中の切り替えであり、アプリのプロセスを終了して起動し直すとオンになる。
データ・形態の精細度・描画処理は前版と同じ。

## ビルドと配布物

- `bundleRelease`、`testDebugUnitTest`、`lintRelease`：成功。
- 単体テスト7件成功。Lintはエラー0、既存の警告17件。
- 同じAABから全データ入りAPKと分割APKセットを生成。
- AABと全データ入りAPKの全711アトラスファイルをマニフェストのSHA-256と照合。
- `bundletool validate`、APK署名、16KB ZIPアラインメントを検証。
- 正確なサイズ・SHA-256：[release-1.0.2.json](play/release-1.0.2.json)。
- 今回は初期値の変更に合わせ、署名付きAPKの起動と切り替えを重点確認。
  前版の計装テスト・元データとの照合・性能測定は再実行していない。
  この版の分割APKセットは生成済みだが、分割インストールの再検証は行っていない。

## エミュレーター

API 35 / x86_64 / SwiftShaderに配布用APKを上書きインストール。

- 起動直後に523細胞の全2,237,417本の枝を表示。
- 表示設定の「神経の概観」スイッチがオンであることをUI階層で確認。
- スイッチをオフにすると概観の枝数が0になり、元の領域モデル7,712,880三角形を表示。
- オフにした後のプロセス再起動で、概観がオンに戻ることを確認。
- version 1.0.2 / code 3、前面Activity、インストール済みAPKのSHA-256一致を確認。
- [起動直後の画面](screenshots/1.0.2/01-default-overview.png)を保存。

## 実機

Pixel 10a（8GB、Android 17）へ、上記で検証した同じAPKを `install -r` で更新。
アプリデータ消去・アンインストール・タッチ入力注入は行っていない。

- インストール成功、version 1.0.2 / code 3、APKハッシュ一致、前面での起動を確認。
- 起動直後の描画ログで `overviewEdges=2237417` を確認。
- 背景の領域は1,613,676三角形、GPU形態バッファは76,283,872 bytes。
  これはアプリ全体のメモリ使用量ではない。

Google Playへのアップロード・審査申請・製品版公開は未実施。
