# Copy to keystore.properties; keep secrets and keys outside version control.
storeFile=/absolute/path/to/your-upload-key.jks
storePassword=REPLACE_LOCALLY
keyAlias=your-upload-key-alias
keyPassword=REPLACE_LOCALLY
